name: KotlinLoader
main: dev.steyn.kotlinloader.bootstrap.KotlinLoaderPlugin
version: ${project.version}
api-version: 1.13
load: STARTUP
author: steenooo
prefix: KotlinLoader
description: PluginLoader to allow Plugins to be written in idiomatic Kotlin.

commands:
  kotlinloader:
    aliases: [kotlin, kotlinl, kl]
    description: Kotlin Loader Plugin Information
    permission: kotlinloader.info
    usage: /kotlinloader